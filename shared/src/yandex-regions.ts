/**
 * Справочник частых регионов Яндекса (lr / Wordstat regions) для всех серверов.
 * НЕ исчерпывающий — только удобные псевдонимы по именам. Любой числовой id
 * Яндекса работает напрямую (resolveOne пропускает цифры как есть), а полный
 * список id можно получить инструментом `wordstat_regions_tree`.
 * XMLStock маппит эти же id и на Google. Расширять только здесь (единый источник).
 */
export const YANDEX_REGIONS: Record<string, number> = {
  // ── Макрорегионы / страны ──
  'россия': 225,
  'беларусь': 149,
  'казахстан': 159,
  'украина': 187,
  'узбекистан': 171,
  'азербайджан': 167,
  'армения': 168,
  'грузия': 169,
  'киргизия': 207,
  'таджикистан': 209,
  'молдова': 208,

  // ── Столицы СНГ ──
  'минск': 157,
  'киев': 143,
  'алматы': 162,
  'астана': 163,

  // ── Россия: города и области ──
  'москва': 213,
  'мск': 213,
  'москва и область': 1,
  'санкт-петербург': 2,
  'спб': 2,
  'питер': 2,
  'санкт-петербург и область': 10174,
  'новосибирск': 65,
  'екатеринбург': 54,
  'екб': 54,
  'казань': 43,
  'нижний новгород': 47,
  'челябинск': 56,
  'самара': 51,
  'омск': 66,
  'ростов-на-дону': 39,
  'ростов': 39,
  'уфа': 172,
  'красноярск': 62,
  'пермь': 50,
  'воронеж': 193,
  'волгоград': 38,
  'краснодар': 35,
  'саратов': 194,
  'тюмень': 55,
  'ижевск': 44,
  'барнаул': 197,
  'иркутск': 63,
  'хабаровск': 76,
  'владивосток': 75,
  'ярославль': 16,
  'махачкала': 28,
  'томск': 67,
  'оренбург': 48,
  'кемерово': 64,
  'рязань': 11,
  'астрахань': 37,
  'пенза': 49,
  'липецк': 9,
  'тула': 15,
  'киров': 46,
  'калининград': 22,
  'курск': 8,
};

function resolveOne(part: string): string {
  const p = part.trim();
  if (/^\d+$/.test(p)) return p;
  const id = YANDEX_REGIONS[p.toLowerCase()];
  if (id === undefined) {
    throw new Error(
      `Неизвестный регион «${p}» — передай числовой id региона Яндекса ` +
      `(полный список — инструмент wordstat_regions_tree) или одно из имён: ${Object.keys(YANDEX_REGIONS).join(', ')}`,
    );
  }
  return String(id);
}

/** «Москва, Санкт-Петербург» / «213,2» → ['213','2']; undefined → undefined (все регионы). */
export function resolveRegionIds(region?: string): string[] | undefined {
  if (!region) return undefined;
  return region.split(',').map(resolveOne);
}

/** Один регион (первый из списка) числом — для API, принимающих единственный lr. */
export function resolveRegionId(region?: string): number | undefined {
  const ids = resolveRegionIds(region);
  return ids ? Number(ids[0]) : undefined;
}
